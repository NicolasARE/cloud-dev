describe("Placeholder", () => {

    test("placholder test", async () => {
        // Arrange
        const a = 1;
        const b = 2;

        // Act
        const c = a + b;

        // Assert
        expect(c).toBe(3);
    });

});