apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: db-auth-pvc
  namespace: todo-app
spec:
  resources:
    requests:
      storage: 5Gi
  volumeMode: Filesystem
  accessModes:
    - ReadWriteOnce
